# Capture notes

`01_BASELINE_CONTEXT.png` is the restored-source baseline. `02_CANDIDATE_A_SCALE_CONTEXT.png`
shows transform-only height reduction. `03_CANDIDATE_B_MATERIAL_CONTEXT.png`
shows the independently justified material response. `04_SELECTED_REMOVAL_CONTEXT.png`
shows the selected removal experiment.

All four were captured through the existing headed P1-R14 driver, with no new
instrumentation. The first wrapper attempt timed out before producing a
manifest; the warmed direct existing route then produced PASS manifests. That
failed attempt is not used as visual evidence.
