# P1 LOAD-01 review pack

This pack is the repository-side index for the exact-head LOAD-01 evidence. The real gameplay-scale screenshots, phase manifests, resource timing ledgers, and Godot logs are retained on D: under:

`D:\CodexData\evidence\ascendant-realms-p1-load-01\`

The final pack must contain or reference three baseline ordinary-flow runs and three exact-head after runs. `01_LOAD01_PLAYABLE.png` is a real 1920×1080 battlefield capture, not a title card or blank frame. The companion `p1-load-01-validator-report.json` is generated by the dedicated validator.
