# P1 LOAD-01 review pack

This pack is the repository-side index for the exact-head LOAD-01 evidence. The real gameplay-scale screenshots, phase manifests, resource timing ledgers, and Godot logs are retained on D: under:

`D:\CodexData\evidence\ascendant-realms-p1-load-01\`

The final pack contains a real 1920×1080 battlefield frame plus references to three baseline ordinary-flow runs and three exact-head after runs. The final evidence source is `9787a009a64015401f70a48cbc71ac838cd98d01`; `01_LOAD01_PLAYABLE_1920x1080.png` is a real gameplay-scale battlefield capture, not a title card or blank frame. The companion `p1-load-01-validator-report.json` is generated by the dedicated validator.
