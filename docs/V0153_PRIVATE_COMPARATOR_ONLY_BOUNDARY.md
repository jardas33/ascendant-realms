# v0.153 Private Comparator-Only Boundary

v0.153 is limited to the isolated Godot private comparator.

Allowed:

- Add a private `--hybrid-three-slot-composition-stress` dispatch path.
- Add wrappers, npm scripts, docs, tests, and ignored evidence for v0.153.
- Reuse the already-selected Worker, Barracks, and Aster private comparator sources.

Forbidden:

- New AI images.
- New runtime-art slots.
- Existing reference candidate import.
- Normal Salto player-slice mutation.
- No browser runtime wiring.
- Production package inclusion.
- Save, stable-ID, manifest, art-slot registry, or gameplay mutation.
- v0.154 work unless the v0.153 gate passes and the queue explicitly proceeds.
