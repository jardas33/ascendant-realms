# v0.151 Aster Billboard Single-Slot Intake Spec

Status: private comparator-only experiment.

This checkpoint tests exactly one original Aster static billboard cutout in the isolated Godot runtime-art comparator. The source is ignored under `artifacts/desktop-spikes/godot-salto/v0151/local-aster-slot/`, and the tracked fallback is diagnostic geometry only.

Scope:

- Generate exactly one AI image for `aster_billboard_static_v0151`.
- Preserve Aster as the default central Commander / Champion, fight anchor, skill spender, and relic-build identity.
- Use v0.144 T1 as the primary brief reference and v0.144 T3 only as a limited presence cue.
- Do not import, crop, trace, or otherwise use existing reference candidates as runtime art.
- Keep the Worker context locked to `HYBRID_WORKER_TRIMMED_1024`.
- Keep the Barracks context locked to the v0.150 `HYBRID_BARRACKS_768_WRAPSAFE_OFFSET_BLEND` repair.

Acceptance gate:

- Tier S/M/L evidence must include local Aster, fallback, Worker context, Barracks context, and procedural orthographic comparator rows.
- Tier L local-vs-fallback FPS ratio must be at least `0.90`.
- Tier L local-vs-fallback p95 frame-time ratio must be at most `1.15`.
- Aster must read as hero, not Worker.
- Worker must remain distinct.
- Foot pivot, alpha review posture, selection ring, pan/zoom, and scale captures must be reviewable.

Boundaries:

- Private comparator only.
- No normal Salto player-slice mutation.
- No browser runtime wiring.
- No fourth runtime-art slot.
- No production approval, final Aster art approval, final Godot selection, or v0.152 work.
