# v0.124 Performance Smoke Report

The required v0.124 performance evidence is generated by:

```text
npm run godot:validate:player-slice
```

The ignored report path is:

```text
artifacts/desktop-spikes/godot-salto/v0124/performance-smoke.json
```

The smoke covers the player-facing Tier M 2.5D `CLEAN_READABILITY` path, one bounded Ashen pressure wave, Lume activation, input acceptance, objective transition, Results transition, stuck-unit count, package size, and launch posture.

This is not final production performance certification.
