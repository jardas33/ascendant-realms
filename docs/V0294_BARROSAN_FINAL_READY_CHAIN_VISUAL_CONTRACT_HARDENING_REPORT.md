# v0.294 Barrosan Final Ready Chain Visual Contract Hardening

## Scope

Evidence and validation hardening only. No Godot runtime script, state, button, marker, unit, building, or default-runtime behavior is changed.

## Base

- Base HEAD: `561611b7c6ef3f92fd1b0d0d3aafc2fd49ba5c5b`
- Branch: `codex/v0215-v0226-recovery`

## Accepted chain preserved

Commit Engage -> PRESSURE CHECKED / ASHEN BRACED; Hold Line -> LINE HELD / ASHEN CONTAINED; Train -> RESERVE READY; Assign -> RESERVE ASSIGNED; Signal -> BRIDGE SIGNAL SENT / SIGNAL SENT / RESERVE ACK; Prepare -> SUPPORT ORDER READY / ORDER READY; Approve -> DEPLOYMENT APPROVED / APPROVED; Stage -> LAUNCH ORDER STAGED / LAUNCH STAGED; Release Ready -> FINAL RELEASE READY / RELEASE READY.

## Visual contract and idempotence

The dedicated compiler creates a 47-item review pack from retained accepted captures, including the full chain, repeated-action checks, selected-card spacing, global-prompt exclusion, static no-motion proof, contact sheet, and black-frame rejection report. The dedicated validator verifies all exact labels, required v0.293 static proof actions, 47 review artifacts, one RELEASE READY marker/final-strip contract, selected-card layout facts, and the default-runtime/static boundary.

Idempotence is evidenced for Commit Engage, Hold Line, Train, Assign, Signal, Prepare, Approve, Stage, and Release Ready. The chain remains a static, accepted review contract: repeated actions do not add duplicate top-strip states or world markers.

## No-overlap and default-runtime evidence

The retained v0.292 proof facts are required: no stale `Select Aster` onboarding text in selected cards, no global prompt inside a card, separated title/body/status/button rows, and no raw validator prose in the live HUD. The v0.294 validator rejects any `desktop-spikes/` runtime diff relative to the accepted v0.293 base, proving the true default runtime remains unchanged by this checkpoint.

## What did not change

No deployment, movement, pathing, route preview, combat, damage, HP loss, projectiles, death/despawn, AI, waves, fog, economy/resource mutation, or true-default-runtime mutation. The validator rejects runtime/gameplay file changes relative to the v0.293 base.

## Evidence and validation

- Review pack: `artifacts/manual-review/v0294-barrosan-final-ready-chain-visual-contract-hardening/`
- Validator: `tools/godot/saltoV0294BarrosanFinalReadyChainVisualContractHardeningTool.mjs`
- Command: `npm run godot:validate:salto-barrosan-final-ready-chain-visual-contract-hardening`
- Retained ladder: dedicated v0.293 through v0.269 validators and v0.259 UI invariant validator passed.
- Full local suite: 887 tests, production build, content validation, art-intake, runtime-art-slots, artifact retention, `npm run godot:all`, and `git diff --check` passed.

## CI and final repository state

Exact-SHA CI and final clean/sync state are recorded after the v0.294 commit is pushed. This report deliberately does not claim CI success before that remote gate completes.
