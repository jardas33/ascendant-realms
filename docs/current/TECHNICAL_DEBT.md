# Technical debt

- historical version documents are numerous and flat; canonical current docs now live under `docs/current`, `docs/product`, `docs/gameplay`, `docs/art`, `docs/architecture`, `docs/qa`, and `docs/codex`;
- browser behavior, Godot qualification, and evidence tooling need explicit runtime labels in every report;
- normal player evidence must be separated from debug/review overlays;
- dirty worktrees and generated artifacts require stronger ownership manifests;
- broad test/script inventory makes command selection error-prone; task handoffs should name exact commands;
- art provenance and runtime approval must remain distinct from reference-only material;
- player-facing visual quality remains behind the long-term desktop target.
