# v0.154 Private Comparator-Only Boundary

v0.154 is limited to the isolated Godot private comparator.

Allowed:

- Generate exactly one local Militia source image.
- Derive deterministic alpha metadata and cutout from that one source.
- Add a private `--militia-billboard-single-slot` dispatch path.
- Add wrappers, npm scripts, docs, tests, fallback, and ignored evidence for v0.154.
- Reuse the already-selected Aster, Worker, and Barracks private comparator sources as context.

Forbidden:

- Additional AI images.
- Alternate Militia pose, direction, animation frame, or animation system.
- New production runtime-art slot approval.
- Existing reference candidate import.
- Normal Salto player-slice mutation.
- No browser runtime wiring.
- Fifth runtime-art slot creation.
- Production package inclusion.
- Save, stable-ID, manifest, art-slot registry, or gameplay mutation.
- v0.155 work unless the v0.154 gate passes and the queue explicitly proceeds.
