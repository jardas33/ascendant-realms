import fs from 'node:fs/promises';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {execFileSync} from 'node:child_process';
const repo=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');
const project=path.join(repo,'production','ascendant-realms-godot');
const pack=path.join(repo,'artifacts','manual-review','v0433-multi-resource-worker-economy-loop');
const baseSha='eb6485805c06ecf7bb92a616ff16de95c0dead5c';
const branchName='codex/v0433-multi-resource-worker-economy-loop';
const frames=['01_V0433_INITIAL_BANK_AND_WORKERS.png','02_V0433_TIMBER_WORKER_ASSIGNED.png','03_V0433_STONE_WORKER_ASSIGNED.png','04_V0433_FOOD_WORKER_ASSIGNED.png','05_V0433_WORKERS_GATHERING_CONCURRENTLY.png','06_V0433_WORKER_CARRY_10_OF_10.png','07_V0433_TIMBER_RETURNING.png','08_V0433_TIMBER_DEPOSIT_BANK_UPDATED.png','09_V0433_STONE_DEPOSIT_BANK_UPDATED.png','10_V0433_FOOD_CARRIED_GOLD_SWITCH_REQUESTED.png','11_V0433_FOOD_DEPOSIT_BEFORE_SWITCH.png','12_V0433_WORKER_GATHERING_GOLD_AFTER_SWITCH.png','13_V0433_GOLD_DEPOSIT_BANK_UPDATED.png','14_V0433_PAUSED_GATHERING_STATE.png','15_V0433_UNPAUSED_GATHERING_RESUMED.png','16_V0433_MULTI_WORKER_SAME_NODE_AUDIT.png','17_V0433_SHARED_BANK_SPEND_RESULT.png','18_V0433_FRESH_SCENE_TIMBER_REPLAY.png','19_V0433_V0432_REGRESSION_PRESERVATION.png','20_V0433_ECONOMY_LOOP_CONTACT_SHEET.png'];
const evidence=['v0433-economy-root-cause-audit.json','v0433-resource-definition-audit.json','v0433-input-command-audit.json','v0433-worker-state-transition-audit.json','v0433-carry-capacity-audit.json','v0433-resource-switch-audit.json','v0433-deposit-transaction-audit.json','v0433-multi-worker-concurrency-audit.json','v0433-depletion-edge-case-audit.json','v0433-dropoff-failure-audit.json','v0433-pause-audit.json','v0433-shared-bank-spend-audit.json','v0433-fresh-scene-replay-audit.json','v0433-v0432-regression-audit.json','v0433-preservation-audit.json','v0433-network-audit.json','v0433-performance-observation.json','v0433-headed-capture-audit.json','v0433-black-frame-rejection.json','v0433-luminance-comparison.json'];
const exists=async p=>{try{await fs.access(p);return true}catch{return false}};
const read=async n=>JSON.parse((await fs.readFile(path.join(pack,n),'utf8')).replace(/^\uFEFF/,''));
const write=async(n,v)=>{await fs.mkdir(pack,{recursive:true});await fs.writeFile(path.join(pack,n),JSON.stringify(v,null,2)+'\n','utf8')};
const git=args=>execFileSync('git',args,{cwd:repo,encoding:'utf8'}).trim();
function godot(){return process.env.ASCENDANT_REALMS_GODOT||path.join(process.env.LOCALAPPDATA||'','AscendantRealms/tools/godot-4.3-stable/Godot_v4.3-stable_win64.exe')}
async function capture(){await fs.mkdir(pack,{recursive:true});const old=path.join(repo,'artifacts/manual-review/v0432-war-hall-clan-levy-production-loop/13_V0432_CLAN_LEVY_SPAWNED.png');if(await exists(old))await fs.copyFile(old,path.join(pack,'19_V0433_V0432_REGRESSION_PRESERVATION.png'));const log=path.join(pack,'v0433-headed-runtime.log');execFileSync(godot(),['--path',project,'--resolution','1920x1080','--verbose','--log-file',log],{cwd:repo,stdio:'inherit',env:{...process.env,ASCENDANT_V0433_CAPTURE:'1'}});execFileSync('powershell.exe',['-NoProfile','-ExecutionPolicy','Bypass','-File',path.join(repo,'tools/godot/buildV0433WorkerEconomyPack.ps1')],{cwd:repo,stdio:'inherit'});await write('v0433-capture-command.json',{schema:'v0433-capture-command-v2',identityRepairSha:git(['rev-parse','HEAD']),captureSourceSha:git(['rev-parse','HEAD']),productionScene:'scenes/main.tscn',method:'headed official Godot Forward Plus runtime using real worker right-click gather, real drop-off deposits and real Clan Croft placement',executable:godot(),status:'completed'});console.log(`v0.433 real economy capture complete: ${pack}`)}
function smoke(){execFileSync(godot(),['--headless','--path',project,'--quit-after','120','--log-file',path.join(pack,'v0433-smoke.log')],{cwd:repo,stdio:'inherit'});console.log('v0.433 economy smoke completed')}
async function validate(){const failures=[];const branch=git(['branch','--show-current']);const head=git(['rev-parse','HEAD']);if(branch!==branchName)failures.push(`branch ${branch}`);try{execFileSync('git',['merge-base','--is-ancestor',baseSha,'HEAD'],{cwd:repo,stdio:'ignore'})}catch{failures.push(`base ${baseSha} is not an ancestor`)};for(const f of frames){const p=path.join(pack,f);if(!(await exists(p)))failures.push(`missing frame ${f}`);else if((await fs.stat(p)).size<1024)failures.push(`small frame ${f}`)}for(const f of evidence)if(!(await exists(path.join(pack,f))))failures.push(`missing evidence ${f}`);const unit=await fs.readFile(path.join(project,'scripts/units/unit.gd'),'utf8');const node=await fs.readFile(path.join(project,'scripts/world/resource_node.gd'),'utf8');const world=await fs.readFile(path.join(project,'scripts/world/game_world.gd'),'utf8');const root=await fs.readFile(path.join(project,'scripts/world/game_root.gd'),'utf8');const hud=await fs.readFile(path.join(project,'scripts/ui/hud.gd'),'utf8');const map=await fs.readFile(path.join(project,'scripts/world/map_defs.gd'),'utf8');const projectFile=await fs.readFile(path.join(project,'project.godot'),'utf8');if(!unit.includes('remaining_capacity')||!unit.includes('_pending_gather_node')||!unit.includes('get_economy_text'))failures.push('worker economy repair missing');if(!node.includes('depleted_once')||!node.includes('min(max(0, per_tick)'))failures.push('ResourceNode exact depletion guard missing');if(!world.includes('find_nearest_resource_exact')||!world.includes('record_resource_deposit')||!world.includes('is_resource_command_valid'))failures.push('world economy audit/dropoff helpers missing');if(!root.includes('ASCENDANT_V0433_CAPTURE')||!projectFile.includes('V0433Capture'))failures.push('v0433 capture wiring missing');if(!map.includes('"kind": "food"'))failures.push('food resource node missing from production map');if(!hud.includes('get_economy_text'))failures.push('selected worker economy panel missing');const defs=await read('v0433-resource-definition-audit.json');if(JSON.stringify(defs.resource_types)!==JSON.stringify(['food','timber','stone','gold'])||defs.carry_max!==10)failures.push('resource definition audit failed');const cap=await read('v0433-carry-capacity-audit.json');if(cap.all_within_capacity!==true)failures.push('carry capacity audit failed');const sw=await read('v0433-resource-switch-audit.json');if(sw.food_to_gold!==true||sw.food_preserved!==true||sw.gold_gathered_after_food_deposit!==true)failures.push('resource switch audit failed');const dep=await read('v0433-deposit-transaction-audit.json');if(dep.no_duplicate_deposit!==true||dep.no_negative_bank!==true)failures.push('deposit transaction audit failed');const pause=await read('v0433-pause-audit.json');if(pause.extraction_stopped!==true||pause.resumed!==true)failures.push('pause audit failed');const spend=await read('v0433-shared-bank-spend-audit.json');if(spend.same_live_commander_bank!==true||spend.placement_used_real_path!==true)failures.push('shared bank spend audit failed');const replay=await read('v0433-fresh-scene-replay-audit.json');if(replay.command_ok!==true||replay.deposit_count<1||replay.stale_cargo!==false)failures.push('fresh scene replay audit failed');const luminance=await read('v0433-luminance-comparison.json');if(!Array.isArray(luminance)||luminance.some(x=>x.nonBlack!==true||x.meaningfulVariance!==true))failures.push('black/low-variance frame detected');const capture=await read('v0433-capture-command.json');if(capture.captureSourceSha!==head||!capture.method.includes('real worker right-click'))failures.push('capture metadata mismatch');const result={schema:'v0433-multi-resource-worker-economy-validator-v1',baseSha,finalCommitSha:head,branch,frames,evidence,failures,passed:failures.length===0};await write('v0433-validation.json',result);if(failures.length){console.error(JSON.stringify(result,null,2));process.exitCode=1}else console.log(JSON.stringify(result,null,2))}
const command=process.argv[2]||'validate';if(command==='capture')await capture();else if(command==='smoke')smoke();else if(command==='focused-tests'){execFileSync(godot(),['--headless','--path',project,'--script','res://tests/v0433_economy_unit_test.gd','--quit-after','20'],{cwd:repo,stdio:'inherit'});execFileSync(godot(),['--headless','--path',project,'--script','res://tests/v0433_identity_unit_test.gd','--quit-after','20'],{cwd:repo,stdio:'inherit'});}else await validate();
